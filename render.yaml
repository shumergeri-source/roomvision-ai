services:
  - type: web
    name: roomvision
    runtime: node
    plan: free
    buildCommand: echo "No build needed"
    startCommand: node server.js
    envVars:
      - key: OPENROUTER_KEY
        sync: false
      - key: SERPAPI_KEY
        sync: false
      - key: PORT
        value: "10000"
